# Pinned to the 0.3.x line: this is the version that ships the typed-decisions
# checkpoint (`laya.load(..., subfolder="typed-decisions")`) which we depend on.
# Bump the upper bound only after re-testing the smoke harness.
laya>=0.3.0,<0.4.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
# huggingface_hub is needed by the installer for explicit, resumable model
# downloads (so the first laya-server boot does not block on a 1 GB pull).
huggingface_hub>=0.24.0
# psutil lets the doctor report free RAM and disk without shelling out.
psutil>=5.9.0

# -- fase-8-final T3: de facto lock (NOT == pins -- see note below) --------
# Exact versions installed and verified 2026-09-23 (Windows, Python 3.11.9):
#   fastapi==0.139.0  uvicorn==0.42.0  pydantic==2.12.5
#   huggingface_hub==0.36.2  psutil==5.9.8  (laya itself: NOT installed here)
# Why no `==` pins: pinning was NOT verified against a clean-room `pip
# install` (no network install was run in this environment, and mutating the
# shared interpreter is out of scope), so `==` could break resolution for
# other users. Until pins are verified, reproduce with this de facto lock:
#   pip install -r py/requirements.txt && pip freeze | <the five above>
# or pin the five versions above explicitly after testing a fresh venv.
# Node side is already lockfile-pinned: package-lock.json exists, so use
# `npm ci` (exact, reproducible) instead of `npm install` (range-resolving).
# install.sh still runs `npm install`; switching it to `npm ci` is a T4/doc
# decision, not made here.
